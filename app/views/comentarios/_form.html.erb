<%= form_for(@comentario) do |f| %>
    <%= f.hidden_field :usuario_id, :value => current_user.id %>
    <%= f.hidden_field :post_id, :value => 4 %>

    <div class="form-group">
      <%= f.label :corpo, "Título:" %>
      <%= f.text_field :corpo, :class=>"form-control"%>
    </div>

    <div class="form-actions" align="center">
      <%= f.submit "Comentar", :class=>"btn btn-success" %>
    </div>
<% end %>




